table:
  name: restaurant_choice
  schema: public
object_relationships:
  - name: name
    using:
      foreign_key_constraint_on: name_id
array_relationships:
  - name: options
    using:
      manual_configuration:
        column_mapping:
          id: choice_id
        insertion_order: null
        remote_table:
          name: restaurant_option_choice_map
          schema: public
