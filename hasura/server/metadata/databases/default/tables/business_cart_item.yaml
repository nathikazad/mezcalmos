table:
  name: cart_item
  schema: business
select_permissions:
  - role: customer
    permission:
      columns:
        - customer_id
        - id
        - service_id
        - cost
        - service_type
      filter:
        customer_id:
          _eq: X-Hasura-User-Id
