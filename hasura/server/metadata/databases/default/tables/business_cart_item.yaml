table:
  name: cart_item
  schema: business
select_permissions:
  - role: customer
    permission:
      columns:
        - item_details_id
        - customer_id
        - id
        - parameters
      filter:
        customer_id:
          _eq: X-Hasura-User-Id
