table:
  name: stripe_info
  schema: public
object_relationships:
  - name: service_provider_payment_info
    using:
      foreign_key_constraint_on:
        column: stripe_info_id
        table:
          name: service_provider_payment_info
          schema: public
