table:
  name: home_rental
  schema: business
object_relationships:
  - name: rental
    using:
      foreign_key_constraint_on: rental_id
select_permissions:
  - role: customer
    permission:
      columns:
        - address
        - bathrooms
        - bedrooms
        - gps_location
        - home_type
        - rental_id
      filter: {}
