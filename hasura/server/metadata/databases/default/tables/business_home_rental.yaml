table:
  name: home_rental
  schema: business
object_relationships:
  - name: rental
    using:
      foreign_key_constraint_on: rental_id
select_permissions:
  - role: customer
    permission:
      columns:
        - rental_id
        - bedrooms
        - bathrooms
        - gps_location
        - home_type
      filter: {}
