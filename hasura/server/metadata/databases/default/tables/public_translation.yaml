table:
  name: translation
  schema: public
array_relationships:
  - name: translations
    using:
      foreign_key_constraint_on:
        column: translation_id
        table:
          name: translation_value
          schema: public
select_permissions:
  - role: anonymous
    permission:
      columns:
        - shared
        - id
      filter: {}
  - role: user
    permission:
      columns:
        - id
        - shared
      filter: {}
